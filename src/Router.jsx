import {BrowserRouter, Route, Routes} from "react-router";

const Router = () => {
    return <>
        <BrowserRouter>
            <Routes>
                <Route path="/" element={"sxasx"} />
            </Routes>
        </BrowserRouter>,
    </>
}

export default Router